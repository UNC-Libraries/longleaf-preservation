# JRuby JAR dependencies resolved via jar-dependencies / Maven Central.
# Run `bundle exec lock_jars` after adding or changing entries here.

jar 'io.ocfl:ocfl-java-core', '2.2.3'

# Suppress SLF4J "No providers found" warnings from ocfl-java (and any other
# Java libraries that use SLF4J). Replace with slf4j-simple or logback-classic
# if you want ocfl-java log output routed somewhere.
jar 'org.slf4j:slf4j-simple', '2.0.17'
